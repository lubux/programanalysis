var el = document.getElementById(tagName);

/** @type {string} */
var key = el.getAttribute("data-event-id");
/** @type {string} */
var options = el.getAttribute("data-theme");
/** @type {string} */
var map = el.getAttribute("data-rest-endpoint");
/** @type {string} */
var se = el._getAttribute_();